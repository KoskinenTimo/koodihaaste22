import styled from "styled-components";

export const BasicVotesCardWrapper = styled.div`
  padding: 1em;
  justify-content: space-between;
  background-color: ${(props) => props.theme.background};
  margin: 1em auto;
`